name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'playground/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install root dependencies
        run: npm ci --legacy-peer-deps

      - name: Build library
        run: npm run build

      - name: Install React playground dependencies
        working-directory: playground/react
        run: npm install

      - name: Build React playground
        working-directory: playground/react
        run: npm run build

      - name: Install Vue playground dependencies
        working-directory: playground/vue
        run: npm install

      - name: Build Vue playground
        working-directory: playground/vue
        run: npm run build

      - name: Prepare deploy folder
        run: |
          mkdir -p deploy/react deploy/vue
          cp playground/home/index.html deploy/
          cp -r playground/react/dist/* deploy/react/
          cp -r playground/vue/dist/* deploy/vue/

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: deploy

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
